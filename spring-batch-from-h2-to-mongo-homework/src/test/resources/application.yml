spring:
  batch:
    job:
      enabled: false
  shell:
    interactive:
      enabled: false
  jpa:
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:HW
    driverClassName: org.h2.Driver
    username: sa
    password:

  data:
    mongodb:
      port: 0
      database: library